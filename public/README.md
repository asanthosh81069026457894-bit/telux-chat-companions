// Tiny inline favicon — a lime square on a transparent background.
// Served as /favicon.ico so browsers that always request /favicon.ico
// don't 404. Real icon work lives in the marketing team's design files.